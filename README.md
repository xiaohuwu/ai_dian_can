# ai_dian_can

基于 FastAPI、Vue、LangChain 和 Pinecone 的智能点餐系统。

## 本地运行

后端：

```bash
source venv/bin/activate
python run.py
```

前端：

```bash
cd ui
npm install
npm run dev
```

运行前请在项目根目录配置 `.env`。API Key、数据库密码等敏感配置不得提交到仓库。
